{
  "name": "clouderp-platform",
  "version": "1.0.0",
  "description": "CloudERP Fullstack MERN Application",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\" \"python ml-service/app.py\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
